import {
    CATHERINE
} from 'constants/scene/actors'
import {
    WEEPING,
    QUIPPING,
    PUZZLED
} from 'constants/scene/actors/catherine'

const INSTANCES = [
    {
        actor: CATHERINE,
        instance: WEEPING
    },
    {
        actor: CATHERINE,
        instance: QUIPPING
    },
    {
        actor: CATHERINE,
        instance: PUZZLED
    }
]

export default INSTANCES
