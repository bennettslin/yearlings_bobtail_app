module.exports = {
    plugins: {
        // Inline import rules.
        'postcss-import': {},
        // Polyfills, including vendor prefixes.
        'postcss-preset-env': {},
        cssnano: {}
    }
}
