export const IS_SCROLLING_SELECTOR = (
    { scrollOverlayStore: { isScrolling } }
) => isScrolling

export const LYRIC_SCROLL_TOP_SELECTOR = (
    { scrollOverlayStore: { lyricScrollTop } }
) => lyricScrollTop
