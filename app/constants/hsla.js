import {
    _TEST_KEY
} from './hslaKeys'

const
    _TEST = {
        h: 0,
        s: 0,
        l: 90,
        a: 0.8
    },

    HSLA_MAP = {
        [_TEST_KEY]: _TEST
    }

export {
    HSLA_MAP
}
