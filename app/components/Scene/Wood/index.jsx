import React, { memo } from 'react'
import cx from 'classnames'

const Wood = memo(() => (
    <div
        className={cx(
            'Wood',
            'abF'
        )}
    />
))

export default Wood
