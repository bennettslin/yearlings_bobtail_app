module.exports = {

    getTotalWordsInAnnotations: function(annotationsObject) {
        var objectKeys = Object.keys(annotationsObject);

        return objectKeys.reduce(function() {
            // TODO.
        }, 0);
    }
}