import { PRETEEN_BENNETT } from '../../keys'
import { MAT } from '../../keys/instances/preteenBennett'

const ARRANGEMENTS_ACTORS = {
    [PRETEEN_BENNETT]: {
        [MAT]: {
            yIndex: 5,
            arrangement: {
                xFloat: 5.5,
                xWidth: 4,
                zHeight: 2
            }
        }
    }
}

export default ARRANGEMENTS_ACTORS
