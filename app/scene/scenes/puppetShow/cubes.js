import { PUPPET_SHOW_KEY } from 'scene/cubesKeys'

const
    PUPPET_SHOW_CUBES = {
        // Just panels.
        floor: {
            zIndices: [
                [1]
            ]
        }
    },

    CUBES = {
        [PUPPET_SHOW_KEY]: PUPPET_SHOW_CUBES
    }

export default CUBES
